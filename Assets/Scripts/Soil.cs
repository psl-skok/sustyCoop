using UnityEngine;

public class Soil : MonoBehaviour
{
    /*public GameObject flowerPrefab; // Assign your flower prefab in the Inspector
    private bool isPlanted = false;

    void OnMouseDown()
    {
        if (!isPlanted)
        {
            PlantFlower();
        }
    }

    private void PlantFlower()
    {
        // Instantiate a flower at this soil's position
        GameObject flower = Instantiate(flowerPrefab, transform.position, Quaternion.identity);
        flower.GetComponent<Flower>().SetSoil(this); // Link flower to this soil
        isPlanted = true; // Prevent further planting
    }

    public void ResetSoil()
    {
        isPlanted = false; // Allow replanting if needed
    }*/
}
